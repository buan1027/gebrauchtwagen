# gebrauchtwagen

Ein FastAPI-Projekt fuer eine Gebrauchtwagen-Anwendung.

## Start

```powershell
uv sync
uv run uvicorn gebrauchtwagen.main:app --app-dir src --reload
```

## Tests

```powershell
uv run pytest
```

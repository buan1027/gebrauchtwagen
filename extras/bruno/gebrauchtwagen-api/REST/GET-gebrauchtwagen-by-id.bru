meta {
  name: GET gebrauchtwagen by ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/gebrauchtwagen/1
  body: none
  auth: none
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 200
  res.body.id: eq 1
}